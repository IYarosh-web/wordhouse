import { createBrowserHistory } from "history";

import { unstable_HistoryRouter } from "react-router-dom";

export const history = createBrowserHistory();
export const BrowserRouter = unstable_HistoryRouter;
